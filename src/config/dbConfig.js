// Configuration file for database connection

const dbConfig = {
  host: 'localhost',
  port: 5432,
  user: 'postgres',
  password: 'postgress',
  database: 'postgres'
};

module.exports = dbConfig;
